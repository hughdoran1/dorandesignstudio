# Keep the Railway build context lean. NOTE: do NOT exclude assets the Dockerfile COPYs
# (graph-schema.svg, the .png icons, content/) — those must reach the build.
node_modules/
DDSWebstie2026/
MetaBallBlend/
content_backup/
# files/  <- NOT ignored: the Dockerfile COPYs it (logo + trophy). Ignoring it fails the build.
files.zip
.claude/
.DS_Store
.git/
*.py
*.pdf
*.ai
prototype.html
guides.html
guide.html
grid-anim-test.html
grid-system.html.bak
extract_prototype.py
# Schemavisualisation.png  <- NOT ignored: content/manifest.json declares it as the journal cover for `graffold-assets`
IconGenerator.png.orig
